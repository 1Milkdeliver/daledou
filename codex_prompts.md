# Codex 运维指令模板（复制即用）

> 在 `D:\Deepseek Harness\daledou` 目录下启动 codex 后，把下面任一条直接粘给它。
> 它会自动读取 `AGENTS.md`，知道项目背景、命令和安全红线。

## 日常检查

```
检查今天的运行日志 log/1206423023/，汇总：哪些任务成功了、哪些失败了、失败原因是什么。有异常就详细说明。
```

```
定时任务运行正常吗？用 schtasks 查一下三个任务的最近运行结果，有问题就修复。
```

## 上游更新 / 冲突处理

```
执行 update.ps1 检查上游更新。如果 log/update.log 里有冲突或失败，按 AGENTS.md 的 git 工作流处理冲突，处理完跑冒烟测试确认一切正常。
```

```
上游最近更新了什么？对比一下本地和 origin/main 的差异，总结新提交涉及哪些任务/文件，有没有需要我注意的（比如新增任务或改配置）。
```

## 游戏新活动 → 写新任务

```
游戏出了新活动「活动名」。我抓包拿到了接口，内容如下：
[在这里粘贴：请求参数 cmd=xxx&op=xxx&id=xxx，以及返回的 HTML 片段]
请按 AGENTS.md 的"如何新增游戏任务"流程，帮我写好任务函数并注册，然后跑单任务测试（只测试这个新任务，不要跑整个模块）。
```

```
检查一下游戏首页（cmd=index&style=1）有没有本地脚本还没覆盖的新活动入口？只做分析，不要执行任何游戏操作。
```

## 游戏页面改版 / 任务失效

```
任务「xxx」好像失效了，日志显示一直没执行或报错。帮我抓首页和分析该任务页面，定位是页面改版还是接口变了，能修就修，不能修说明原因。
```

## Cookie 更新

```
cookie 好像失效了（日志出现登录/重定向提示）。告诉我怎么重新抓取 cookie 并更新 config/dld_cookie.yaml，注意不要泄露 cookie 内容。
```

## 代码质量 / 对齐上游

```
检查本地和上游 src/tasks 的差异，本地有没有多余改动可以精简的？尽量让本地补丁最小化，方便以后 rebase。
```

## 一键启动（命令行）

```bat
cd /d D:\Deepseek Harness\daledou && codex
:: 或用 codex_run.bat
```

> 提示：codex 交互模式下也可以直接说人话，比如
> "看看今天乐斗跑得怎么样"、"游戏更新了帮我看看新活动"。
