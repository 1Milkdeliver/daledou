# Q宠大乐斗 自动化使用说明

> 基于开源项目 [gaoyuanqi/daledou](https://github.com/gaoyuanqi/daledou)（MIT），
> 通过请求大乐斗**文字版**接口（`https://dld.qzapp.z.qq.com/qpet/cgi-bin/phonepk?cmd=...`）
> 自动完成日常/周常任务，支持多账号、定时调度。

## 已完成的配置

| 项目 | 状态 |
| --- | --- |
| 代码 | `D:\Deepseek Harness\daledou`（已 clone，依赖已安装） |
| Cookie | `config/dld_cookie.yaml`（QQ 1206423023，已验证有效 ✅） |
| 冒烟测试 | `noon.邪神秘宝` 成功领取免费抽奖 ✅ |
| 全量测试 | `uv run main.py noon` 全模块运行 ✅ |
| 定时任务 | `daledou-noon`（每天 13:05）、`daledou-evening`（每天 20:05）、`daledou-update`（每天 09:00 订阅上游更新）、`daledou-snapshot`（每天 22:00 只读快照 + 研究数据包更新）、`daledou-experiments`（每天 13:00 免费差分实验） |

## 常用命令（在 daledou 目录下）

```bat
uv run main.py -h                        :: 查看所有模块和任务
uv run main.py noon.邪神秘宝             :: 执行单个任务（所有账号）
uv run main.py noon                      :: 执行 noon 模块全部任务
uv run main.py evening                   :: 执行 evening 模块全部任务
uv run main.py 1206423023.noon.登录有礼  :: 指定账号的指定任务
uv run main.py                           :: 启动内置定时守护进程（13:01 / 20:01）
```

## 执行结果

- 日志：`log/1206423023/2026-08-20.log`（loguru 按天、按账号记录）
- 统计：每次运行结束会打印 `成功：N | 失败：M` 和失败原因
- 定时任务输出：`log/scheduler_noon.log`、`log/scheduler_evening.log`

## 任务机制（重要，扩展时必读）

1. 脚本先请求首页 `cmd=index&style=1`
2. 检查首页 HTML 里是否存在 `>任务名<` 的链接文本
3. 存在 → 执行对应注册函数；不存在 → 静默跳过（游戏活动下架后自动失效）

**注册名 = 首页链接文本**，不能随意改。

## 如何用 codex / AI 编码代理扩展新任务

游戏每次更新活动，都可以让编码代理按下面流程补任务：

1. **抓包**：浏览器打开文字版页面（`https://dld.qzapp.z.qq.com/qpet/cgi-bin/phonepk?cmd=index&style=1`），
   F12 → Network → 勾选 Preserve log，手动点一遍新活动，记录每次请求的
   `cmd=xxx&op=xxx&id=xxx` 参数和返回 HTML。
2. **看首页链接文本**：新活动入口的 `<a ...>活动名</a>` 文本就是注册名。
3. **写任务函数**：在 `src/tasks/noon.py`（或 evening.py）添加：

```python
@register()  # 函数名 = 首页链接文本
async def 新活动名(d: DaLeDou):
    await d.get("cmd=xxx&op=yyy")            # 打开活动页
    d.log(d.find(r"</p>(.*?)<br />"))        # 记录操作结果
    # 解析 HTML 里的按钮/参数，逐个 d.get(...) 完成操作
```

4. **加配置**（可选）：`config/default.yaml` 对应模块下加键，代码里用 `d.config("键.子键")` 读取。
5. **测试**：`uv run main.py noon.新活动名`
6. 交给 codex 时，把「抓包得到的请求参数 + 返回 HTML 片段 + 上面的任务模板」作为 prompt，
   它就能照着现有代码风格生成完整任务。

## 自动订阅上游更新

本地仓库 `origin` 已指向上游 [gaoyuanqi/daledou](https://github.com/gaoyuanqi/daledou)，
每天 09:00 由 `daledou-update` 定时任务自动执行 `update.ps1`：

1. `git fetch origin --tags` 拉取上游最新代码
2. 有更新 → `git rebase origin/main` 合并，**本地补丁（超时重试、调度脚本、文档）自动保留**
3. 合并后跑冒烟测试（`uv run main.py -h`，无游戏副作用）
4. 结果写入 `log\update.log`；rebase 冲突时停止并提示，不会自动覆盖

上游新增/修改的任务代码会随更新自动合入，无需手动处理。
上游的浏览器端"订阅"（GitHub Watch）需要 GitHub 账号，在仓库页面点 Watch 即可。

## 注意

- ⚠️ Cookie 就是账号凭证，**不要外传/贴到公开渠道**；泄露后需重新登录获取。
- 脚本设计为**不消耗鹅币/斗豆/斗币**（对可能扣费的入口有前置判断），但页面改版可能失效，
  新任务请先小范围验证。
- 系统时区需为上海时间（当前已是 China Standard Time ✅）。
- 定时任务只在**电脑开机时**触发；想 24 小时跑需保持电脑开机。
- 删除定时任务：`schtasks /Delete /TN daledou-noon /F`、`schtasks /Delete /TN daledou-evening /F`
